---
# Ansible Inventory for Marketing Analytics Platform

all:
  children:
    metabase_servers:
      hosts:
        s60:
          ansible_host: 100.91.164.109
          ansible_user: pavel
          ansible_python_interpreter: /usr/bin/python3
          docker_network: metabase_net
          services:
            - metabase
            - metabase-db
            - prefect-server
            - prefect-worker
            - dbt
          volumes:
            - metabase-db-data
            - prefect-data
